1. Pairing rule written three times. find_portamentos, Validator.check_portamento, and MusicXml.slideAnchor each encode which notes a !port! connects, and abc2xml already disagrees on grace notes. The altitude reviewer's proposal: run expand_portamentos before abc_to_xml too, so abc2xml's existing -(/-) handling emits the pair and slideAnchor, mkNotations, insertElem, addNotations and afterNotations are deleted. Pro: two copies instead of three, and the one with the least reason to exist goes. Con: the abc2xml round-trip test then exercises expansion rather than abc2xml itself.
2. Expansion as a preprocessing step. Fold expand_portamentos into finish_preprocessing, returning the map from there, so all four call sites go through one place and the two _column_map discards in exporter.py disappear. Con: finish_preprocessing gains a return value every caller must unpack.
3. Apply the map at the reader instead of rewriting SVG files. Carry ColumnMap on SvgTune beside header_line_count and map at svgrenderer.py:234, deleting rewrite_svg_annotation_columns. Pro: one parser of <abc> tags and no write-back per page. Con: the cached SVG no longer stands on its own.
4. Severity.INFO. Nothing produces an INFO Diagnostic, so the error_marks.py guard and two comments defend against an absent case. Options: ReportedLine.severity becomes Optional[Severity], or INFO is filtered where diagnostics are produced.
5. ReportedLine and Abc2midiRun. Production reads only any(severity is X). Reducing to a per-line severity function plus a worst-severity return would drop both dataclasses, but test_abc_midi_export.py asserts on .text and on purity, so that test changes.
6. Reuse across the wider codebase. The abnormal-exit message string exists in five places with one program name each. The severity-driven status bar block in easy_abc.py:1012 still scans app_state.messages for the word "Error", the defect this diff fixed for abc2midi. Four hand-rolled queue_number plus CallLater debounces exist, including the new one in abc_assist_panel.py. voice_change_pattern overlaps abc_tune.voice_pattern but they differ on [V: without an id.
7. Assist panel trigger. The panel could bind its own editor's update event instead of ScoreView reaching into frame.abc_assist_panel.
8. Test helper. The two case-loop tests in test_abc_parser.py share an identical body. Extracting a helper is a test rewrite, which your rules gate on your approval.
